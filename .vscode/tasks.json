{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "process",
            "command": "cargo",
            "args": [
                "build"
            ],
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build",
            "label": "cargo_build",
            "options": {
                "cwd": "${workspaceRoot}"
            }
        }
    ]
}